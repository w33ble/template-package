{
  "name": "<%= package %>",
  "version": "0.0.0",<% if (private) { %>
  "private": true,<% } %>
  "description": "<%= description %>",
  "main": "src/index.js",
  "author": "<%= name %> (https://github.com/<%= username %>)",<% if (repository) { %>
  "repository": "<%= repository %>",<% } %>
  "license": "MIT"<% if (!private && public) { %>,
  "publishConfig": {
    "access": "public"
  }<% } %>
}
